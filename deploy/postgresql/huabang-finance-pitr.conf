# Managed by deploy/install_postgres_pitr.sh. archive_mode requires a cluster
# restart; all other settings are intentionally conservative for this host.
archive_mode = on
archive_command = '/usr/local/lib/huabang/archive_postgres_wal.sh "%p" "%f"'
archive_timeout = 900
