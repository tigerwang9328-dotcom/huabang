# 华邦老板经营指挥台最终验收记录

验收日期：2026-07-14 至 2026-07-15（北京时间）  
验收范围：三阶段经营快照、经营日报、规则与任务闭环、VIP 行动、通知、移动端及生产运维。

> 自动化测试证明代码契约成立，不替代老板对真实经营数据、责任人和行动结果的人工签字。

## 当前结论

- AI安全边界：固定验收场景覆盖缺费用、缺客流、过期库存和模型不可用；缺失值不会按零参与结论，费用不完整时禁止输出确定性盈亏。
- 任务审计链：状态机覆盖草稿、人工确认、派发、反馈、复查退回、再次反馈、复查通过和关闭；任务、反馈、复查分别保留责任人、时间、证据和幂等请求号。
- Web与移动一致性：两端都读取 `dm.dm_boss_daily_report`，并复用 `get_command_center_snapshot`，同一业务日只允许显示精度不同。
- 退货金额：经营总览、经营日报及移动端统一读取百胜小票支付明细 `baison_pos.refund_amount`；无退货时显示 0 且状态为 `ready`，数据未同步时才显示 `stale`，不再固定标记“待接入”。
- 技术验收：后端、前端、迁移、构建、生产接口、桌面/移动页面和前端回滚机制均已有实测证据。
- 运行观察：2026-07-15 已完成 04:00 销售/退货、05:30 库存、06:30 日报和 06:40 健康检查的真实完整周期。05:30 与 06:30 首次执行分别暴露业务日期字符串绑定和公司级 VIP 规则空参数类型问题，均补回归测试、修复、全量回归并在同一周期幂等重跑成功；06:40 返回 `healthy=true`、`issues=[]`，未创建健康告警草稿。
- 人工签字：待老板完成 30 秒经营判断与一条真实行动任务闭环验收。
- 人工抽查入口已更新：R001 [ID 276](https://hbreare.com/app/warning?exception_id=276)、[ID 277](https://hbreare.com/app/warning?exception_id=277)、[ID 278](https://hbreare.com/app/warning?exception_id=278)；R005 [ID 219](https://hbreare.com/app/warning?exception_id=219)、[ID 220](https://hbreare.com/app/warning?exception_id=220)、[ID 221](https://hbreare.com/app/warning?exception_id=221)；R006 [ID 283](https://hbreare.com/app/warning?exception_id=283)、[ID 284](https://hbreare.com/app/warning?exception_id=284)、[ID 285](https://hbreare.com/app/warning?exception_id=285)；R010 [ID 286](https://hbreare.com/app/warning?exception_id=286)、[ID 287](https://hbreare.com/app/warning?exception_id=287)、[ID 288](https://hbreare.com/app/warning?exception_id=288)；R023 当前仅有 [ID 289](https://hbreare.com/app/warning?exception_id=289)。VIP 行动仍为 [任务 1715](https://hbreare.com/app/task/1715)。归属稽核当前没有真实异常，因为七类归属来源尚未全部就绪，不能伪造样本补验。

## AI安全边界

固定场景必须满足：

1. 费用缺失时，经营利润值为空、状态为 `pending_data`，结论不得写“公司盈利/亏损”。
2. 客流没有可靠来源时，该指标不进入 AI 上下文，不得按 0 解释。
3. 库存过期时只进入限制说明，不进入已就绪事实。
4. 模型不可用时使用确定性模板；规则证据保留，建议动作仍为草稿并要求人工确认。

## 任务审计链

归属异常和 VIP 行动使用同一闭环：

`draft -> pending -> feedback_submitted -> processing(复查退回) -> feedback_submitted -> review_passed -> closed`

验收时必须能查到：原始证据、确认人、责任人、截止日期、反馈内容、复查人、复查结果、关闭人、各节点时间和工作流版本。通知失败不得回滚已经确认的任务。

生产基线共有 599 条任务：594 条草稿、5 条待处理；其中原有 VIP 行动草稿 550 条。当前没有真实反馈、复查或关闭记录，因此自动状态机和字段契约已经通过，但真实业务任务的完整审计链仍待人工执行和签字，不能用测试数据冒充。

首选真实闭环样本为任务 `1715`（`MV202607141820070F8E29`）：会员 `000344279445`、门店 `185805`、大额余额长期未消费、截止 2026-07-17。该任务当前没有候选导购；负责人接口仅降级返回 3 个 `mapping_pending` 系统账号。钉钉员工档案中“代积芳”属于“商品部”，“王小虎”属于公司根部门，两人均没有工号、岗位或门店 `185805` 映射，不能据此自动认定门店负责人。主管需先在 `/app/system/users` 将真实员工账号绑定到门店 `185805`，再在 `/app/task/1715` 确认责任人和联系话术，完成派发、反馈、复查及关闭。

## Web与移动一致性

同一业务日逐项核对：销售额、实收金额、库存金额、VIP 余额、重大异常数、待处理任务数和退货金额。Web `/dashboard/overview` 与移动 `/mobile/command-center` 均读取同一经营快照；前端只负责格式化，不重新计算业务指标。

2026-07-13 生产接口逐字段结果：销售 13,455 元、实收 8,820 元、库存金额 2,401,938.52 元、VIP 余额 1,118,709.26 元、重大异常 7 项、待处理任务 38 项、退货金额 0 元；Web 与移动返回的数值、状态、来源和时间全部相同。

退货金额补充样本：2026-07-06 为 54 元、退货率 0.4%；2026-07-12 和 2026-07-13 均为 0 元。三日状态均为 `ready`，来源均为 `baison_pos.refund_amount`。

2026-07-15 04:00 百胜小票自动同步完成后，2026-07-14 共 17 张有效小票，负结算金额为 0 元。06:30 日报快照生成前，经营总览已直接使用同一完整同步的实时指标，桌面和移动视口均显示业务日期 `2026-07-14`、退货金额 0 元、退货率 0.0%、状态 `ready`、来源 `baison_pos.refund_amount`；未完成同步时仍保持 `stale`，不会把缺数据误报为 0。

2026-07-14 正式经营快照结果：销售 8,521 元、实收 7,519 元、退货 0 元、17 单、33 件、客单价 501.24 元、连带率 1.94、毛利 6,346.32 元；库存使用 2026-07-15 当日 10 编码外穿衣物快照，共 18,504 件、2,365,867.97 元，VIP 正余额 1,117,707.26 元。销售、退货、库存和 VIP 余额核心来源均为 `ready`；费用仍为 `pending_data`，不输出确定性经营利润。

经营快照的库存指标读取当日 05:30 后最新快照；R006/R010 规则证据则固定记录经营日对应的历史库存 DWS，便于以后按原业务日复核。两者日期用途不同，验收时不得把历史规则证据误认为当日实时库存。毛利率状态已与自身来源状态统一：有销售且成本完整时为 `ready`，有销售但成本不完整时为 `estimated`，零销售时为 `pending_data`。

2026-07-15 09:19 生产重算后，`/dashboard/overview?stat_date=2026-07-14` 返回毛利率 74.48%、状态 `ready`，退货金额状态 `ready`。上述 13 个异常 ID 的详情接口均返回 200，每条均有 1 条原始证据并能下钻；未登录访问证据接口返回 401。任务 1715 返回 200，状态仍为 `draft`、负责人为空，真实人工闭环状态未被技术验收改写。

第二轮来源审计补充：2026-07-14 八类费用覆盖率为 0，钉钉财务表仅有 3 条 6 月报销记录，其中 2 条仍在审批中，没有可用于 7 月14日的完整核准费用。归属七类来源中门店交易、跨店历史、退款记录 3 类为 `ready`；抖音订单级绑定、已核验责任导购、导购到门店排班、改单前后日志 4 类为 `pending_data`。这些缺口均是原始来源缺失，不以会员注册门店、考勤部门或最终小票结果推定责任人。

## 生产抽查

| 页面/接口 | 验收重点 | 状态 |
| --- | --- | --- |
| 经营总览 | 核心指标、退货金额、重大异常、今日行动 | 页面与接口通过 |
| 经营日报 | 来源时间、状态、14 日经营记录 | 页面与接口通过 |
| 门店/商品/库存 | 下钻、筛选、排序、外穿衣物口径 | 页面加载与接口通过 |
| 会员/VIP | 余额、分层、行动候选和责任归属 | 页面加载与接口通过 |
| 异常/任务 | 证据、人工确认、反馈、复查、关闭 | 页面与技术状态机通过；真实闭环待签字 |
| AI/移动端 | 安全回退和同源快照 | 通过 |

## 自动化与运维证据

| 验收项 | 实测结果 |
| --- | --- |
| 第三阶段聚焦测试 | `tests/test_phase3_acceptance.py` 7 passed |
| 后端全量测试 | 490 passed，1 skipped，1 个既有 Pydantic 弃用警告 |
| 数据库迁移 | `222d5e6f7081 (head)`；第二阶段已完成 `downgrade -1 -> upgrade head`，关键业务计数不变 |
| 前端测试 | 131 passed |
| 类型检查/生产构建 | `vue-tsc --noEmit` 通过；Vite 生产构建通过 |
| Playwright | 10 个经营路由、桌面/移动共 20 组，0 个失败；退货补充验收桌面/移动 2 组均为业务日期 `2026-07-14`、金额 0 元、退货率 0.0%；无登录跳转、空白主区、页面级横向溢出或 API 4xx/5xx |
| 前端版本回滚 | 候选版本切换 HTTP 200，恢复原版本 HTTP 200，原版本 `index.html` 哈希恢复一致 |
| 服务健康 | 后端 active；健康接口、数据库、Redis 均正常 |
| 库存恢复验证 | 05:30 自动拉取完成后修复日期类型并按同一锁入口重跑成功；10 个白名单编码、82,233 条记录、原始库存 24,356 件，外穿衣物 18,504 件，DWS 10 编码合计与统一视图一致 |
| 定时任务日志 | 04:00 小票成功；05:30 库存修复后 06:10 成功；06:30 日报修复后 06:33 成功，第二次幂等重跑新增任务草稿为 0；06:40 健康检查 `healthy=true`、`issues=[]`，`HEALTH-20260714` 数量为 0 |

截图与机器可读结果保存在 `D:\huabang\output\playwright\final-acceptance` 和 `D:\huabang\output\playwright\refund-acceptance`。

## 人工签字

### 30 秒预读答案（2026-07-14）

1. 昨日销售 8,521 元、实收 7,519 元、退货 0 元、毛利 6,346.32 元；17 单、33 件，客单价 501.24 元，连带率 1.94。
2. 销售、退货、毛利、库存和 VIP 余额已就绪；库存库龄因历史入库批次匹配不全仍为 `estimated`；费用和经营利润为 `pending_data`，不得判断公司净盈利或亏损。
3. 当前优先核查负库存：门店 134681 有 3 个负库存 SKU（[证据 ID 284](https://hbreare.com/app/warning?exception_id=284)），门店 285702 和仓库 GYNG 各有 1 个；同时 285702、285102、285101 的历史库存周转天数偏高。
4. VIP 行动 [任务 1715](https://hbreare.com/app/task/1715) 尚未指定真实负责人，截止 2026-07-17。当前候选导购为空，负责人接口只返回 3 个门店映射待补账号；钉钉档案也没有可证明属于门店 `185805` 的员工。老板或主管需先在 `/app/system/users` 绑定真实员工与门店 `185805`，再派发、反馈、复查和关闭；当前不能把“责任人是谁”回答完整，因此最终业务签字仍待完成。

老板应在打开首页后 30 秒内回答：

1. 昨天销售、实收、退货和毛利结果如何？
2. 哪些指标是预估、过期或待接入？
3. 当前最大的经营风险是什么，证据在哪里？
4. 今天由谁负责处理，截止时间是什么？

| 人工验收项 | 当前状态 | 签字/日期 |
| --- | --- | --- |
| 30 秒经营判断 | 待验收 |  |
| 抽查重大异常原始证据 | 待验收；R001、R005、R006、R010 各有 3 条直达样本；R023 当前仅 1 条真实样本 ID 289，未补造另外 2 条 |  |
| 完成一条真实 VIP 任务全闭环 | 待验收；使用 [任务 1715](https://hbreare.com/app/task/1715) |  |
| 归属稽核 | 待数据源就绪；当前真实归属异常为 0，不制造验收数据 |  |
| 确认 Web 与移动同日指标一致 | 待验收 |  |

老板签字：________________  
签字日期：________________
