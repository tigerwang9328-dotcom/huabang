# 华邦老板经营指挥台最终验收记录

验收日期：2026-07-14 至 2026-07-15（北京时间）  
验收范围：三阶段经营快照、经营日报、规则与任务闭环、VIP 行动、通知、移动端及生产运维。

> 自动化测试证明代码契约成立，不替代老板对真实经营数据、责任人和行动结果的人工签字。

## 当前结论

- AI安全边界：固定验收场景覆盖缺费用、缺客流、过期库存和模型不可用；缺失值不会按零参与结论，费用不完整时禁止输出确定性盈亏。
- 任务审计链：状态机覆盖草稿、人工确认、派发、反馈、复查退回、再次反馈、复查通过和关闭；任务、反馈、复查分别保留责任人、时间、证据和幂等请求号。
- Web与移动一致性：两端都读取 `dm.dm_boss_daily_report`，并复用 `get_command_center_snapshot`，同一业务日只允许显示精度不同。
- 退货金额：经营总览、经营日报及移动端统一读取百胜小票支付明细 `baison_pos.refund_amount`；无退货时显示 0 且状态为 `ready`，数据未同步时才显示 `stale`，不再固定标记“待接入”。
- 技术验收：后端、前端、迁移、构建、生产接口、桌面/移动页面和前端回滚机制均已有实测证据。
- 运行观察：2026-07-14 晚间百胜库存接口连续 3 次 `RemoteProtocolError` 后，已为传输错误增加最多 3 次短退避重试；2026-07-15 03:27 手工全量同步成功，10 个编码、82,233 条记录、原始库存 24,356 件均完整。仍需观察 04:00 销售、05:30 库存和 06:30 日报的自动完整周期。
- 人工签字：待老板完成 30 秒经营判断与一条真实行动任务闭环验收。

## AI安全边界

固定场景必须满足：

1. 费用缺失时，经营利润值为空、状态为 `pending_data`，结论不得写“公司盈利/亏损”。
2. 客流没有可靠来源时，该指标不进入 AI 上下文，不得按 0 解释。
3. 库存过期时只进入限制说明，不进入已就绪事实。
4. 模型不可用时使用确定性模板；规则证据保留，建议动作仍为草稿并要求人工确认。

## 任务审计链

归属异常和 VIP 行动使用同一闭环：

`draft -> pending -> feedback_submitted -> processing(复查退回) -> feedback_submitted -> review_passed -> closed`

验收时必须能查到：原始证据、确认人、责任人、截止日期、反馈内容、复查人、复查结果、关闭人、各节点时间和工作流版本。通知失败不得回滚已经确认的任务。

生产基线共有 588 条任务：583 条草稿、5 条待处理；其中 VIP 行动草稿 550 条。当前没有真实反馈、复查或关闭记录，因此自动状态机和字段契约已经通过，但真实业务任务的完整审计链仍待人工执行和签字，不能用测试数据冒充。

## Web与移动一致性

同一业务日逐项核对：销售额、实收金额、库存金额、VIP 余额、重大异常数、待处理任务数和退货金额。Web `/dashboard/overview` 与移动 `/mobile/command-center` 均读取同一经营快照；前端只负责格式化，不重新计算业务指标。

2026-07-13 生产接口逐字段结果：销售 13,455 元、实收 8,820 元、库存金额 2,401,938.52 元、VIP 余额 1,118,709.26 元、重大异常 7 项、待处理任务 38 项、退货金额 0 元；Web 与移动返回的数值、状态、来源和时间全部相同。

退货金额补充样本：2026-07-06 为 54 元、退货率 0.4%；2026-07-12 和 2026-07-13 均为 0 元。三日状态均为 `ready`，来源均为 `baison_pos.refund_amount`。

## 生产抽查

| 页面/接口 | 验收重点 | 状态 |
| --- | --- | --- |
| 经营总览 | 核心指标、退货金额、重大异常、今日行动 | 页面与接口通过 |
| 经营日报 | 来源时间、状态、14 日经营记录 | 页面与接口通过 |
| 门店/商品/库存 | 下钻、筛选、排序、外穿衣物口径 | 页面加载与接口通过 |
| 会员/VIP | 余额、分层、行动候选和责任归属 | 页面加载与接口通过 |
| 异常/任务 | 证据、人工确认、反馈、复查、关闭 | 页面与技术状态机通过；真实闭环待签字 |
| AI/移动端 | 安全回退和同源快照 | 通过 |

## 自动化与运维证据

| 验收项 | 实测结果 |
| --- | --- |
| 第三阶段聚焦测试 | `tests/test_phase3_acceptance.py` 7 passed |
| 后端全量测试 | 475 passed，1 skipped，1 个既有 Pydantic 弃用警告 |
| 数据库迁移 | `222d5e6f7081 (head)`；第二阶段已完成 `downgrade -1 -> upgrade head`，关键业务计数不变 |
| 前端测试 | 126 passed |
| 类型检查/生产构建 | `vue-tsc --noEmit` 通过；Vite 生产构建通过 |
| Playwright | 10 个经营路由、桌面/移动共 20 组，0 个失败；无登录跳转、空白主区、页面级横向溢出或 API 4xx/5xx |
| 前端版本回滚 | 候选版本切换 HTTP 200，恢复原版本 HTTP 200，原版本 `index.html` 哈希恢复一致 |
| 服务健康 | 后端 active；健康接口、数据库、Redis 均正常 |
| 库存恢复验证 | 重试补丁后手工全量同步成功；10 个白名单编码、82,233 条记录、原始库存 24,356 件，未误删白名单或旧批次 |
| 定时任务日志 | 销售、会员、06:30 日报有历史成功记录；2026-07-15 自动完整周期待复验 |

截图与机器可读结果保存在 `D:\huabang\output\playwright\final-acceptance`。

## 人工签字

老板应在打开首页后 30 秒内回答：

1. 昨天销售、实收、退货和毛利结果如何？
2. 哪些指标是预估、过期或待接入？
3. 当前最大的经营风险是什么，证据在哪里？
4. 今天由谁负责处理，截止时间是什么？

| 人工验收项 | 当前状态 | 签字/日期 |
| --- | --- | --- |
| 30 秒经营判断 | 待验收 |  |
| 抽查一条重大异常的原始证据 | 待验收 |  |
| 完成一条真实 VIP/归属任务全闭环 | 待验收 |  |
| 确认 Web 与移动同日指标一致 | 待验收 |  |

老板签字：________________  
签字日期：________________
