# 华邦老板经营指挥台第二阶段综合验收记录

验收日期：2026-07-14（北京时间）  
验收范围：异常证据链、费用与利润门禁、VIP 分层、VIP 唤醒任务闭环、任务通知失败隔离及 Alembic 一步回滚恢复。

> 本文区分“自动化验收”与“老板业务签字”。自动测试通过只证明代码契约成立，不替代老板对真实异常、财务核准表和真实 VIP 唤醒任务的人工验收。

## 验收结论

- 自动化契约：异常结构、利润门禁、VIP 分层证据、任务状态机、通知失败不丢任务及一步迁移回滚已有自动验证证据。
- 实时数据：异常 45 条、异常证据 18 条；最新异常业务日为 2026-07-13。生产未删除任务已扩展为 588 条（583 条草稿、5 条待处理），当前反馈和复查记录均为 0。
- 业务签字：尚未完成。当前没有足够真实记录支持“每个已启用规则抽查 3 条”，财务费用也尚未核准完整，未执行并签字确认一条真实 VIP 唤醒任务的草稿到关闭流程。
- 发布判断：第二阶段自动化门禁可复验；在下方三项老板人工签字完成前，不应宣称第二阶段业务验收通过，也不得据此输出确定性的经营利润结论。

## 异常证据链

### 自动化验收证据

- `test_every_rule_engine_rule_has_three_traceable_evidence_samples` 为规则引擎每条注册规则构造 3 个固定验收样本，逐条验证：事实、阈值、规则版本、证据哈希、原始记录键、来源表和下钻路由均存在，且稳定来源键不重复。
- `test_inventory_warning_rules_persist_threshold_evidence_and_source_contract` 验证 `R016-R021` 由库存预警流水持久化，表结构和生成 SQL 均包含 `rule_id / thresholds / evidence / source_name`。
- 以上固定样本只用于验证结构与计算契约，不冒充真实业务异常。

### 2026-07-15 实时数据抽查基线

`dm.dm_exception_audit` 中带版本化证据的规则计数：

| 规则 | 异常数 | 证据数 | 是否达到 3 条 |
| --- | ---: | ---: | --- |
| R001 | 5 | 5 | 是 |
| R005 | 5 | 5 | 是 |
| R006 | 3 | 3 | 是 |
| R007 | 1 | 1 | 否 |
| R010 | 4 | 4 | 是 |

`dm.dm_inventory_warning` 中第二阶段库存规则计数：

| 规则 | 记录数 | 阈值/证据/来源状态 |
| --- | ---: | --- |
| R006 | 5 | 有证据和来源；该规则阈值为空对象 |
| R007 | 430 | 完整 |
| R016 | 131 | 完整 |
| R017 | 69 | 完整 |
| R018 | 44 | 完整 |
| R019 | 153 | 完整 |
| R020 | 38 | 完整 |
| R008 / R021 | 0 | 当前快照未触发，不能伪造业务样本 |

另有 27 条 `RULE_Rxxx` 旧异常没有独立证据行，以及 548 条旧库存预警没有规则号、阈值、证据和来源；这些旧数据不能计入第二阶段“可追溯异常”验收样本。最新业务日 2026-07-13 的 `Rxxx` 记录已使用版本化证据链，不受旧记录影响。

已为人工抽查固定以下 3 条真实样本，入口均为 `/app/warning`，在业务日期选择 `2026-07-13` 后按规则或门店查找并点击“证据明细”：

| 异常 ID | 规则 / 对象 | 结论 | 原始证据 |
| ---: | --- | --- | --- |
| 217 | R001 / 门店 134681 | 销售 978 元，低于 3,000 元阈值 | `dws.dws_store_daily`，`sales=978`、`threshold=3000`，来源时间 2026-07-14 15:43（北京时间） |
| 226 | R006 / 门店 285702 | 1 个负库存 SKU | `dws.dws_inventory_daily`，`negative_sku_count=1`，来源时间 2026-07-14 05:39（北京时间） |
| 231 | R010 / 门店 185808 | 周转约 247 天，高于 120 天阈值 | `dws.dws_inventory_daily`，`inventory=88634.47`、`sell_through_days=247`、`threshold=120`，来源时间 2026-07-14 05:39（北京时间） |

**当前结论：待老板抽查。** 对有 3 条以上真实记录的规则，应在页面逐条下钻到原始业务记录；对不足 3 条或当前未触发的规则，应使用经老板确认的固定业务样本补验，不能用虚构记录补足数量。

## 利润门禁

### 自动化验收证据

固定财务核准样本：

- 净销售额 2,000 元，销售成本 700 元。
- 八类必需费用均为已核准实际数，每类 100 元，费用合计 800 元。
- 自动计算毛利 1,300 元、经营利润 500 元，状态为 `ready`，与手工公式 `2,000 - 700 - 800` 一致。

费用不完整样本：

- 缺少 `other` 费用时，仍显示毛利 1,300 元。
- `missing_expense_types` 明确返回 `other`。
- 经营利润为 `None`，状态为 `pending_data`，AI 利润结论门禁返回 false。

### 实时数据状态

2026-07-13 公司及 7 家门店毛利状态均为 `ready`，但八类费用覆盖率为 0，`finance_approved=false`，经营利润状态为 `pending_data`。因此当前系统只能展示毛利和缺失费用来源，不能给出公司是否盈利的确定结论。

**当前结论：待财务与老板签字。** 需要财务提供同日期核准表，确认八类费用的金额、归属期间和核准人后，再逐项核对系统经营利润。

## VIP 分层与任务闭环

### 自动化验收证据

固定 VIP 样本验证：

- 当前余额来自 `baison.CZ_DQJE`，样本余额 8,000 元。
- 高价值、高余额、沉睡、消费频率下降、充值后未二次消费等标签和风险均保留规则版本与计算证据。
- 唤醒候选责任状态为 `unconfirmed`，候选导购只作为候选，不自动冒充已确认负责人。
- 状态机完整覆盖：草稿 → 派发 → 反馈 → 复查退回 → 再反馈 → 复查通过 → 关闭。
- 任务确认接口测试模拟钉钉发送失败；任务先持久化为 `pending` 再发送通知，返回通知失败但任务、确认人和确认时间均保留。

### 实时数据状态

- 会员分层日期：2026-07-14。
- 分层会员：11,643 人；有标签 11,016 人；有风险 564 人；唤醒候选 550 人。
- 当前任务：草稿 583 条，已派发待处理 5 条；反馈 0 条、复查 0 条。
- 尚未选择一名真实 VIP 并由实际负责人完成草稿到关闭的全流程，因此不能记录为业务闭环已签字。

建议人工验收样本：`/app/task/1715`，任务号 `MV202607141820070F8E29`，会员 `000344279445`，门店 `185805`，原因“大额余额长期未消费”，截止 2026-07-17。该任务仍为草稿且未指定责任人，必须由主管确认真实责任人及话术后再派发；不得由自动化测试代替业务执行。

**当前结论：待老板选定真实 VIP 唤醒任务并全程验收。**

## 迁移回滚

### 自动化验收证据

执行命令：

```bash
cd /srv/huabang-ai-center/.worktrees/boss-command-center-three-phase/backend
/srv/huabang-ai-center/backend/.venv/bin/alembic downgrade -1
/srv/huabang-ai-center/backend/.venv/bin/alembic upgrade head
/srv/huabang-ai-center/backend/.venv/bin/alembic current
```

执行结果：

- 成功从 `222d5e6f7081` 回退到 `211c4d5e6f70`，再升级到 `222d5e6f7081 (head)`。
- 回滚前通知发件箱为 0 条，因此本次演练未丢失待发通知。
- 回滚前后：异常 45 → 45、异常证据 18 → 18、会员分层 11,643 → 11,643、未删除任务 38 → 38。
- 最新一步迁移只回退每收件人通知发件箱，不删除异常、异常证据、会员分层或任务主体。

## 自动化验收证据

| 验证项 | 命令/范围 | 结果 |
| --- | --- | --- |
| 第二阶段相关聚焦测试 | 异常、利润、VIP、任务、通知、权限 10 个测试文件 | 95 passed，1 个既有 Pydantic 弃用警告 |
| 第二阶段综合验收测试 | `tests/test_phase2_acceptance.py` | 7 passed，1 个既有 Pydantic 弃用警告 |
| Alembic 一步回滚 | `downgrade -1` → `upgrade head` → `current` | 通过，最终为 `222d5e6f7081 (head)` |
| 历史数据恢复 | 异常、证据、会员分层、任务计数核对 | 45 / 18 / 11,643 / 38，回滚前后不变 |

## 老板人工签字

签字状态：待老板签字

| 人工验收项 | 操作要求 | 当前状态 | 签字/日期 |
| --- | --- | --- | --- |
| 异常证据链 | 打开 `/app/warning`，优先抽查 ID 217、226、231；核对事实、阈值、来源和原始记录下钻 | 样本已准备，待老板确认业务结论 |  |
| 经营利润 | 用财务核准表核对同日八类费用和经营利润 | 待验收；当前费用覆盖率为 0 |  |
| VIP 分层 | 抽查高价值、高余额、沉睡及唤醒候选的原始证据 | 待验收 |  |
| VIP 唤醒任务 | 打开 `/app/task/1715`，完成真实责任人确认、派发、反馈、复查和关闭，并观察一次通知失败重试 | 待验收；自动状态机已验证，真实业务未签字 |  |
| 迁移恢复 | 确认回滚演练记录和历史数据计数 | 待老板确认 |  |

老板签字：________________  
签字日期：________________
