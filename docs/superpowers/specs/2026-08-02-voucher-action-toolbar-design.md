# 凭证快捷操作栏布局

## 目标

改善录凭证页快捷操作栏的视觉层级，不改变任何凭证数据、校验或工作流。

## 已批准设计

- “添加分录、复制上一行、收款分录、付款分录、自动找平”位于左侧操作组。
- “借方、贷方、差额、平衡状态”作为独立汇总组靠右显示。
- 小屏幕下两组自然换行，保持一致间距，汇总组不与操作按钮混排。

## 验收

- 所有现有按钮事件、禁用条件和汇总计算保持不变。
- 宽屏截图中操作按钮左对齐、汇总右对齐。
- 前端类型检查和生产构建通过。
